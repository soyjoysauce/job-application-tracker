{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "node -e \"const fs=require('fs');const path=require('path');const entry=require.resolve('helmet');const dir=path.dirname(entry);console.log('[diag] helmet entry:',entry);console.log('[diag] version:',JSON.parse(fs.readFileSync(path.join(dir,'package.json'),'utf8')).version);console.log('[diag] files:',fs.readdirSync(dir).join(' '))\" && npm run build"
}
